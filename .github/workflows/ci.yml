name: CI

on:
  push:
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    # Surface a wedged test run as a clean failure at ~10min instead of
    # GitHub's 6h default — see #246 for the original 24-min hang.
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.2.20"

      - name: Install deps
        run: bun install

      - name: Cache ergo binary
        uses: actions/cache@v5
        with:
          path: var/ergo-bin
          key: ergo-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('bin/install-ergo') }}

      - name: Install ergo
        run: bin/install-ergo

      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck

      - name: Shellcheck
        run: shellcheck bin/*

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      - name: Test with coverage
        run: bun test --coverage

      - name: Shell tests — start-dispatcher
        run: bash test/start-dispatcher_test.sh

      - name: Shell tests — stop-dispatcher
        run: bash test/stop-dispatcher_test.sh

      - name: Shell tests — spawn
        run: bash test/spawn_test.sh

      - name: Shell tests — agents
        run: bash test/agents_test.sh

      - name: Shell tests — compact-hook
        run: bash test/compact-hook_test.sh

      - name: Shell tests — version
        run: bash test/version_test.sh

      - name: Shell tests — harness
        run: bash test/harness_test.sh

      - name: Shell tests — harness-codex
        run: bash test/harness-codex_test.sh

      - name: Shell tests — registry
        run: bash test/registry_test.sh

      - name: Shell tests — cross-org
        run: bash test/cross-org_test.sh

      - name: Shell tests — signet
        run: bash test/signet_test.sh

      - name: Shell tests — init
        run: bash test/init_test.sh
